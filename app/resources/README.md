Bundled by PyInstaller as a data directory (see eFloStop_Production_Tool.spec
and build_installer.py). Kept in git because PyInstaller aborts the build if the
path does not exist. Drop icons/app.ico here to give the .exe an icon.
